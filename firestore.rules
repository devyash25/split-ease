rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permanent open access for personal use
      allow read, write: if true;
    }
  }
}
